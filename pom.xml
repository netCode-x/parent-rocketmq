<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <!--父用 dependencyManagement 管版本 + dependencies 放通用依赖；子模块显式写自己需要的 <dependencies>-->

  <groupId>org.repo</groupId>
  <artifactId>parent-rocketmq</artifactId>
  <version>1.0</version>
  <packaging>pom</packaging>


  <!-- ========== 聚合：声明子模块 ========== -->
  <modules>
    <module>repo-rocketmq</module>
  </modules>

  <!-- ========== 统一属性管理 ========== -->
  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <spring.boot.version>3.5.0</spring.boot.version>
    <lombok.version>1.18.30</lombok.version>
    <rocketmq.version>4.9.7</rocketmq.version>
    <springdoc.version>2.8.0</springdoc.version>
  </properties>

  <!-- ========== 依赖管理：只声明版本，不实际引入 ========== -->
  <dependencyManagement>
    <dependencies>
      <!-- Spring Boot BOM -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- 内部子模块（可选，将来加模块时用） -->
      <dependency>
        <groupId>org.repo</groupId>
        <artifactId>repo-rocketmq</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- 第三方依赖版本 -->
      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>${lombok.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.rocketmq</groupId>
        <artifactId>rocketmq-client</artifactId>
        <version>${rocketmq.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>${springdoc.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <!-- ========== 所有子模块共用的依赖 ========== -->
  <dependencies>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <!-- ========== 插件管理 ========== -->
  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring.boot.version}</version>
          <executions>
            <execution>
              <goals>
                <goal>repackage</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

</project>